"""Quant core — Black-Scholes, IV solver, Greeks, payoff, scenario, POP, Monte Carlo.

Implemented in M3. Every function here MUST ship with textbook-referenced unit tests
(see apps/engine/tests/test_quant.py) before it is relied on anywhere else.
"""
